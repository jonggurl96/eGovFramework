# server
server:
  port: 8080

# import config
spring:
  config:
    import:
      - "classpath:/cmmn/cache/cache.yml"
      - "classpath:/cmmn/idgnr/idgnr.yml"
      - "classpath:/persistence/dbconfig.yml"

# log4j2.yml import
logging:
  config: "src/main/resources/cmmn/logging/log4j2.yml"
