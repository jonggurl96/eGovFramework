# server
server:
  port: 8080

# import config
spring:
  config:
    import:
      - "classpath:/cmmn/cache/cache.yml"
      - "classpath:/cmmn/idgnr.yml"
      - "classpath:/persistence/dbconfig.yml"

