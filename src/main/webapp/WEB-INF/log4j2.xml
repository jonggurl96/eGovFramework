<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
    <!-- eGovFramework 4.1.0 -->
    <!-- Spring boot 2.7.13 log4j2.xml -->
    <Properties>
        <Property name="LOG_EXCEPTION_CONVERSION_WORD">%xwEx</Property>
        <Property name="LOG_LEVEL_PATTERN">%5p</Property>
        <Property name="LOG_DATEFORMAT_PATTERN">yyyy-MM-dd HH:mm:ss.SSS</Property>
        <Property name="CONSOLE_LOG_PATTERN">%clr{%d{${sys:LOG_DATEFORMAT_PATTERN}}}{faint} %clr{${sys:LOG_LEVEL_PATTERN}} %clr{%pid}{magenta} %clr{---}{faint} %clr{[%15.15t]}{faint} %clr{%-40.40c{1.}}{cyan} %clr{:}{faint} %m%n${sys:LOG_EXCEPTION_CONVERSION_WORD}</Property>
        <Property name="FILE_LOG_PATTERN">%d{${LOG_DATEFORMAT_PATTERN}} ${LOG_LEVEL_PATTERN} %pid --- [%t] %-40.40c{1.} : %m%n${sys:LOG_EXCEPTION_CONVERSION_WORD}</Property>
    </Properties>

    <!-- Appender, Layout 설정 -->
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="${sys:CONSOLE_LOG_PATTERN}" charset="${sys:CONSOLE_LOG_CHARSET}"/>
        </Console>
        <File name="file" fileName="./logs/file/sample.log" append="false">
            <PatternLayout pattern="${sys:CONSOLE_LOG_PATTERN}"/>
        </File>
        <RollingFile name="rolling" fileName="./logs/rolling/sample.log" filePattern="./logs/rolling/sample.%i.log">
            <PatternLayout pattern="${sys:CONSOLE_LOG_PATTERN}"/>
        </RollingFile>
        <JDBC name="jdbc" tableName="db_log">
            <ConnectionFactory class="org.egovframe.rte.fdl.logging.db.EgovConnectionFactory" method="getDatabaseConnection"/>
            <Column name="eventDate" isEventTimestamp="true"/>
            <Column name="logger" pattern="%logger"/>
            <Column name="level" pattern="%level"/>
            <Column name="message" pattern="%msg"/>
            <Column name="exception" pattern="${LOG_EXCEPTION_CONVERSION_WORD}"/>
        </JDBC>
    </Appenders>

    <!-- Logger 설정 -->
    <Loggers>
        <Logger name="exampleLogger" level="DEBUG" additivity="false">
            <AppenderRef ref="console"/>
            <AppenderRef ref="file"/>
        </Logger>
        <Root level="INFO">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>